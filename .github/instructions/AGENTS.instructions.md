---
applyTo: '**'
---
You will always run the command `bash` when you first start a terminal session to change the current shell to bash. Then you will run `conda activate keypoints` to activate the conda environment for this project. After that, you can run any other commands as needed.

Always run command by command in a separate code block. Do not combine multiple commands in a single code block.
